native = term.current()
monitor = peripheral.find("monitor")

term.redirect(monitor)

width,height = term.getSize()

function drawMenu()
	term.clear()
	term.setCursorPos(1,1)

	background_img = 
end

